# Edge Function Secrets for Local Development
# Copy this file to .env and customize as needed
#
# Usage:
#   cp .env.example .env
#
# NOTE: The PDS requires HTTPS. Get the ngrok URL with:
#   cd infra/pds && ./local-dev.sh url

# PDS Configuration for Local Development
# Start local PDS with: cd infra/pds && ./local-dev.sh start
# Then update PDS_HOST with the ngrok URL shown
PDS_HOST=https://<your-ngrok-subdomain>.ngrok-free.app
PDS_ADMIN_PASSWORD=localdevpassword

# For production, these would be:
# PDS_HOST=https://pds.trainers.gg
# PDS_ADMIN_PASSWORD=<your-production-admin-password>
