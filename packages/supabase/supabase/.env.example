# This file is symlinked to the root .env.local by postinstall.sh
# All environment variables live in a single place: <repo-root>/.env.local
#
# The variables below are used by Supabase config.toml (via env()) and edge functions.
# Add them to your root .env.local file.

# PDS Configuration for Local Development
# Edge functions reach PDS via Docker networking (no tunnel needed)
# Start local PDS with: pnpm pds:start
PDS_HOST=http://host.docker.internal:3001
PDS_ADMIN_PASSWORD=localdevpassword
PDS_HANDLE_DOMAIN=localhost

# ngrok Static Domain (optional, for OAuth testing)
# See .env.ngrok.example at repo root for setup instructions
# NGROK_STATIC_DOMAIN=your-domain.ngrok-free.app

# OAuth Provider Credentials (Local Development)
# Discord: https://discord.com/developers/applications
SUPABASE_AUTH_EXTERNAL_DISCORD_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_DISCORD_SECRET=
# GitHub: https://github.com/settings/developers
SUPABASE_AUTH_EXTERNAL_GITHUB_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GITHUB_SECRET=
