# Dashboard Redesign v2 — QA Issues

**Date:** 2026-03-30
**QA By:** Claude (QA Agent)
**Spec:** `docs/superpowers/specs/2026-03-30-dashboard-redesign-v2.md`
**Last Updated:** 2026-04-01 (run 5 — post-implementation data wiring fixes)

---

## Summary

All 5 originally reported issues have been **resolved**. No new issues found.

| #   | Issue                                     | Severity | Status    |
| --- | ----------------------------------------- | -------- | --------- |
| 1   | Alt Switcher Global Filter                | Critical | **FIXED** |
| 2   | Alt Switcher Subtitle Repeats Username    | High     | **FIXED** |
| 3   | Notifications "Mark all read" Missing     | Medium   | **FIXED** |
| 4   | Tournaments Page Heading "My Tournaments" | Low      | **FIXED** |
| 5   | Tournament Manage Page Layout Broken      | Medium   | **FIXED** |

---

## Resolved Issues

### 1. Alt Switcher Now Functions as Global Filter — FIXED

**Location:** `apps/web/src/components/dashboard/dashboard-sidebar.tsx` (lines 149-158, 224-242)

**Fix:** Implemented cookie-based persistence:

- `dashboard-alt` cookie (1-year expiry, path `/`, SameSite lax)
- Selecting an alt sets the cookie via `document.cookie`, then calls `router.refresh()`
- Selecting "All Alts" clears the cookie (`max-age=0`)
- Server components read the cookie via `cookies()`:
  - `dashboard/layout.tsx` (line 37) — passes `selectedAltUsername` to sidebar
  - `dashboard/page.tsx` (line 9) — filters home page data
  - `dashboard/tournaments/page.tsx` (line 21) — filters tournament data
- Cookie value validated against user's actual alts (falls back to null/"All Alts" if invalid)

This matches the spec requirement: "Global filter — selection persists across all dashboard pages via cookie (SSR-compatible)."

---

### 2. Alt Switcher Subtitle No Longer Repeats Username — FIXED

**Location:** `apps/web/src/components/dashboard/dashboard-sidebar.tsx`

**Fix:** Non-main alts now show "Alt" as the subtitle instead of repeating the username.

| Context               | Name              | Subtitle   | Status            |
| --------------------- | ----------------- | ---------- | ----------------- |
| All Alts selected     | All Alts          | "All alts" | Correct           |
| Main alt selected     | admin_trainer     | "Main alt" | Correct           |
| Non-main alt selected | admin_trainer_vgc | "Alt"      | Correct (was bug) |

---

### 3. Notifications Popover Now Has "Mark all read" — FIXED

**Location:** `apps/web/src/components/dashboard/notifications-popover.tsx` (lines 251-266)

**Fix:** Header row now shows "Notifications" title on the left and "Mark all read" button on the right. The button:

- Calls `markAllNotificationsRead` mutation on click
- Disables when `unreadCount === 0`
- Uses `text-primary` color when active, muted when disabled

---

### 4. Tournaments Page Heading Now Says "Tournaments" — FIXED

**Location:** `apps/web/src/app/(dashboard)/dashboard/tournaments/`

**Fix:** Heading changed from "My Tournaments" to "Tournaments" to match sidebar nav and spec.

---

### 5. Tournament Manage Page Renders Properly in Dashboard Shell — FIXED

**Location:** `apps/web/src/app/(dashboard)/dashboard/community/[communitySlug]/tournaments/[tournamentSlug]/manage/`

**Fix:** The tournament manage page now renders correctly within the dashboard layout:

- Proper header with tournament name, status badge ("In Progress"), and action buttons (View Public Page, Audit Log, Settings)
- Tab navigation (Overview, Players, Live)
- Overview tab shows: Live status, "Start Round 1" CTA, Registration Status, Round Progress, Tournament Format, Schedule
- No dark background, no "Tournament not found" errors
- Back arrow navigates to community tournaments list

---

## Verified Passing

| Area                          | Status | Notes                                                                                                                                      |
| ----------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------ |
| Sidebar nav items             | Pass   | "Alts", "Tournaments", no "Inbox"                                                                                                          |
| Sidebar structure             | Pass   | Settings pinned at bottom, user footer, communities section                                                                                |
| Dashboard home page           | Pass   | "Welcome back" heading, stats cards (Win Rate, Rating, Record, Tournaments), recent results                                                |
| Alts page table layout        | Pass   | Table with expandable rows, correct columns, "+ New Alt" button                                                                            |
| Tournaments page filters      | Pass   | Filter chips, dropdowns, summary stats, empty state, heading says "Tournaments"                                                            |
| `/dashboard/history` redirect | Pass   | Correctly redirects to `/dashboard/tournaments`                                                                                            |
| Community Overview            | Pass   | Stats row (3 tournaments, 136 players, 10 staff, founded), "Live Now" card, tournaments list, activity feed                                |
| Community Staff               | Pass   | Kanban drag-and-drop: Unassigned (10 staff), Owner, Admins, Head Judges, Judges columns, search, Add Staff                                 |
| Community Settings            | Pass   | General tab with logo upload, name, description (48/500 char count), social links, Save. Permissions/Notifications tabs present (disabled) |
| Tournament Manage             | Pass   | Full management UI: header with status badge, tabs (Overview/Players/Live), registration stats, round progress, format info, schedule      |
| Alt Switcher popover          | Pass   | "Switch alt" header, alt list with Main badge, checkmarks, separators, "Create new alt", "Manage alts"                                     |
| Alt Switcher persistence      | Pass   | Cookie-based (`dashboard-alt`), survives navigation, read server-side                                                                      |
| Notifications bell icon       | Pass   | Present in header bar, opens popover on click                                                                                              |
| Notifications "Mark all read" | Pass   | Present in popover header, disabled when no unread items                                                                                   |
| NavUser footer                | Pass   | Round avatar, username, role (Player/Owner depending on context), chevron dropdown                                                         |
| Community Request             | Pass   | Welcome section, Discord highlight, social grid, submit button                                                                             |

---

## Additional Bugs Found Post-QA (Session 3)

These bugs were found during data wiring and seed data work after the initial QA runs:

| #   | Issue                                        | Root Cause                                                           | Fix                                                                 | Status    |
| --- | -------------------------------------------- | -------------------------------------------------------------------- | ------------------------------------------------------------------- | --------- |
| 6   | Standings showed 10th for all tournaments    | `Number("1_353")` = NaN as map key                                   | Changed to string key in `getUserTournamentHistory`                 | **FIXED** |
| 7   | Pokemon sprites broken for forms/new Pokemon | Hand-written regex didn't match Showdown URL format                  | Replaced with `getPokemonSprite()` from `@trainers/pokemon/sprites` | **FIXED** |
| 8   | Home page Record always 0-0                  | `getMyDashboardData` ordered by nonexistent `updated_at` column      | Changed to `end_time`                                               | **FIXED** |
| 9   | Home page Record still 0-0 after #8 fix      | PostgREST FK ambiguity between `tournament_phases` and `tournaments` | Added explicit FK hint `!tournament_phases_tournament_id_fkey`      | **FIXED** |
| 10  | Home stats all 0 in "All Alts" mode          | `user?.profile?.id` always undefined (JWT hook doesn't set it)       | Derived `mainAltId` from `getCurrentUserAlts()` query               | **FIXED** |
| 11  | Per-alt ELO rating showed "—" on alts page   | `getPlayerRating()` not wired to alt table rows                      | Added per-alt `useSupabaseQuery` for `getPlayerRating()`            | **FIXED** |
| 12  | Rating column label said "Rating" not "ELO"  | Generic label                                                        | Renamed to "ELO"                                                    | **FIXED** |
| 13  | Teams showed 0 on alts page                  | `getTeamsForAlt()` filtered out teams with 0 pokemon                 | Added real Pokemon data to seed teams                               | **FIXED** |
| 14  | ELO ratings not computed from match results  | Matches inserted as `completed` bypassing trigger                    | Call `apply_elo_result()` directly in seed                          | **FIXED** |
| 15  | Seed data not running during db:reset        | Admin DO block inside registrations guard with early `RETURN`        | Moved to separate DO block; matches + ELO to `12_standings.sql`     | **FIXED** |

### Remaining Polish Items

| #   | Issue                                     | Severity | Status |
| --- | ----------------------------------------- | -------- | ------ |
| 16  | Alt detail page is placeholder            | Medium   | Open   |
| 17  | "All Alts" home shows main alt stats only | Low      | Open   |
| 18  | Community activity feed is placeholder    | Low      | Open   |

## Screenshots

| File                                                            | Description                                                 | Run |
| --------------------------------------------------------------- | ----------------------------------------------------------- | --- |
| `.playwright-mcp/screenshots/qa3-community-overview.png`        | Community overview — stats, live card, tournaments list     | 3   |
| `.playwright-mcp/screenshots/qa3-tournament-manage.png`         | Tournament manage — proper layout, tabs, registration stats | 3   |
| `.playwright-mcp/screenshots/qa3-community-staff.png`           | Staff management — kanban columns, drag-and-drop            | 3   |
| `.playwright-mcp/screenshots/qa3-community-settings.png`        | Community settings — general tab, logo upload, form         | 3   |
| `.playwright-mcp/screenshots/rerun-dashboard-home.png`          | Dashboard home with stats                                   | 2   |
| `.playwright-mcp/screenshots/rerun-alt-switcher-popover.png`    | Alt switcher popover open                                   | 2   |
| `.playwright-mcp/screenshots/rerun-alt-subtitle-fixed.png`      | Alt subtitle showing "Alt" (fixed)                          | 2   |
| `.playwright-mcp/screenshots/rerun-tournaments-page.png`        | Tournaments page with "Tournaments" heading                 | 2   |
| `.playwright-mcp/screenshots/rerun-alts-page.png`               | Alts management page                                        | 2   |
| `.playwright-mcp/screenshots/rerun-community-overview.png`      | Community overview                                          | 2   |
| `.playwright-mcp/screenshots/rerun-notifications-popover.png`   | Notifications popover (run 2 — before fix)                  | 2   |
| `.playwright-mcp/screenshots/rerun-tournament-manage.png`       | Tournament manage (run 2)                                   | 2   |
| `.playwright-mcp/screenshots/qa4-dashboard-home.png`            | Dashboard home — stats, recent results, alt switcher        | 4   |
| `.playwright-mcp/screenshots/qa4-tournaments-alt-persisted.png` | Tournaments — alt switcher persisted after navigation       | 4   |
| `.playwright-mcp/screenshots/qa4-alts-page.png`                 | Alts page — table with 3 alts, "+ New Alt" button           | 4   |
| `.playwright-mcp/screenshots/qa4-notifications-popover.png`     | Notifications — "Mark all read" present, disabled           | 4   |
| `.playwright-mcp/screenshots/qa4-community-overview.png`        | Community overview — stats, live card, activity feed        | 4   |
| `.playwright-mcp/screenshots/qa4-community-staff.png`           | Staff kanban — role columns, drag-and-drop                  | 4   |
| `.playwright-mcp/screenshots/qa4-community-settings.png`        | Community settings — general tab, logo, social links        | 4   |
| `.playwright-mcp/screenshots/qa4-tournament-manage.png`         | Tournament manage — header, tabs, registration stats        | 4   |
