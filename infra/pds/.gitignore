# Sensitive files - never commit
.env
.env.local
*.secret
secrets.txt
admin-password.txt

# Fly.io local state
.fly/

# Local development (ngrok + PDS)
.ngrok.pid
.ngrok.log
.ngrok-hostname
.ngrok-url
.ngrok-web-url
.ngrok-tunnels.yml
.pds-local.env
