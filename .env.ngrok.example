# =============================================================================
# ngrok Static Domain Configuration
# =============================================================================
# Each developer gets one free static domain from ngrok.
# This domain never changes, so OAuth redirect URIs stay stable.
#
# Setup:
#   1. Sign up at https://ngrok.com (free tier includes 1 static domain)
#   2. Go to https://dashboard.ngrok.com/domains
#   3. Copy your static domain (e.g., mollusk-amazed-sadly.ngrok-free.app)
#   4. Copy this file to .env.ngrok and fill in your domain
#
# Usage:
#   pnpm setup:ngrok   # Start tunnel with your static domain on port 3000
# =============================================================================

NGROK_STATIC_DOMAIN=your-domain.ngrok-free.app
